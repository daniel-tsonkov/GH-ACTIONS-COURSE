name: 03 - Workflow Runners
on: workflow_dispatch

jobs:
  echo-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Show the OS
        run: |
          echo "RUNNING on a ${{ runner.os }} 
          machine with ${{ runner.arch }} architecture."